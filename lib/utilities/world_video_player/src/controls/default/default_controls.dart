import 'package:flutter/material.dart';

class DefaultContols extends StatelessWidget {
  const DefaultContols({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
