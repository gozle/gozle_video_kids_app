enum Devices {
  phone,
  tablet,
  tv,
}

enum APIState {
  init,
  loading,
  error,
  succses,
}

enum LocalDataKeys {
  token,
  lang,
}

enum HomeAPIState {
  init,
  loading,
  error,
  succses,
  loadingMore,
  errorMore,
}

enum LocalKeys {
  lang,
  themeMode,
  isFirstTime,
}
